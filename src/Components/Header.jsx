import React from "react";

const Header = () => {
    return(
        <div className="ul fixed menu" >
            <div className="ul container centre">
                <h2 className="ui centered header">Contact Manager</h2>
            </div>
        </div>
    );
};

export default Header;